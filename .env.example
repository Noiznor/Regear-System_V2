# Environment Configuration
NODE_ENV=development
PORT=3001

# API Configuration
# In development, the frontend will connect to http://localhost:3001
# In production, set this to your server URL or leave empty for relative URLs
API_BASE_URL=

# Database Configuration (if you upgrade to a database later)
# DATABASE_URL=postgresql://username:password@localhost:5432/maharlika_db
