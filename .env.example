# Railway Environment Configuration
NODE_ENV=production
PORT=8080

# Railway automatically provides these:
# RAILWAY_STATIC_URL - Your app's public URL
# RAILWAY_VOLUME_MOUNT_PATH - Persistent storage path

# Development Configuration
# NODE_ENV=development
# PORT=8080

# API Configuration
# The frontend will automatically use relative URLs in production
API_BASE_URL=

# Optional: Database Configuration (for future upgrades)
# DATABASE_URL=postgresql://username:password@host:port/database
